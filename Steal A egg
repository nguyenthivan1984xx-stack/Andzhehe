--========================================================--
--              BEST HUB TỔNG HỢP
--   Miranda | Lemon | Hop Server | Fix Lag | ICUS MOD
--========================================================--

local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local Workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")

local Player = Players.LocalPlayer

--========================================================--
-- XÓA GUI CŨ
--========================================================--

pcall(function()
    local OldGui = CoreGui:FindFirstChild("BestHubTongHop")
    if OldGui then
        OldGui:Destroy()
    end
end)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "BestHubTongHop"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.DisplayOrder = 999
ScreenGui.Parent = CoreGui

--========================================================--
-- DRAG
--========================================================--

local function MakeDraggable(Object)

    local dragging = false
    local dragStart
    local startPos

    Object.InputBegan:Connect(function(input)

        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then

            dragging = true
            dragStart = input.Position
            startPos = Object.Position

            input.Changed:Connect(function()

                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end

            end)
        end
    end)

    UserInputService.InputChanged:Connect(function(input)

        if not dragging then
            return
        end

        if input.UserInputType == Enum.UserInputType.MouseMovement
        or input.UserInputType == Enum.UserInputType.Touch then

            local delta = input.Position - dragStart

            Object.Position = UDim2.new(
                startPos.X.Scale,
                startPos.X.Offset + delta.X,
                startPos.Y.Scale,
                startPos.Y.Offset + delta.Y
            )
        end
    end)
end

--========================================================--
-- NÚT MỞ MENU
--========================================================--

local OpenButton = Instance.new("TextButton")

OpenButton.Name = "OpenButton"
OpenButton.Size = UDim2.new(0, 48, 0, 48)
OpenButton.Position = UDim2.new(0, 12, 0.5, -24)
OpenButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
OpenButton.Text = "☰"
OpenButton.TextColor3 = Color3.fromRGB(255, 255, 255)
OpenButton.TextSize = 22
OpenButton.Font = Enum.Font.GothamBold
OpenButton.BorderSizePixel = 0
OpenButton.Parent = ScreenGui

local OpenCorner = Instance.new("UICorner")
OpenCorner.CornerRadius = UDim.new(1, 0)
OpenCorner.Parent = OpenButton

MakeDraggable(OpenButton)

--========================================================--
-- MENU
--========================================================--

local Main = Instance.new("Frame")

Main.Name = "Main"
Main.Size = UDim2.new(0, 270, 0, 315)
Main.Position = UDim2.new(0.5, -135, 0.5, -157)
Main.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
Main.BorderSizePixel = 0
Main.Visible = false
Main.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = Main

local Stroke = Instance.new("UIStroke")
Stroke.Color = Color3.fromRGB(80, 80, 80)
Stroke.Thickness = 2
Stroke.Parent = Main

MakeDraggable(Main)

--========================================================--
-- TITLE
--========================================================--

local Title = Instance.new("TextLabel")

Title.Name = "Title"
Title.Size = UDim2.new(1, -20, 0, 45)
Title.Position = UDim2.new(0, 10, 0, 5)
Title.BackgroundTransparency = 1
Title.Text = "BEST HUB TỔNG HỢP"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 19
Title.Font = Enum.Font.GothamBold
Title.Parent = Main

--========================================================--
-- TẠO BUTTON
--========================================================--

local function CreateButton(Text, Position)

    local Button = Instance.new("TextButton")

    Button.Size = UDim2.new(1, -30, 0, 42)
    Button.Position = Position
    Button.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Button.BorderSizePixel = 0
    Button.Text = Text
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 15
    Button.Font = Enum.Font.GothamBold
    Button.AutoButtonColor = true
    Button.Parent = Main

    local Corner = Instance.new("UICorner")
    Corner.CornerRadius = UDim.new(0, 9)
    Corner.Parent = Button

    return Button
end

--========================================================--
-- BUTTONS
--========================================================--

local MirandaButton = CreateButton(
    "Miranda Hub",
    UDim2.new(0, 15, 0, 58)
)

local LemonButton = CreateButton(
    "Lemon Hub",
    UDim2.new(0, 15, 0, 108)
)

local HopButton = CreateButton(
    "Hop Sever",
    UDim2.new(0, 15, 0, 158)
)

local FixLagButton = CreateButton(
    "Fix Lag",
    UDim2.new(0, 15, 0, 208)
)

local ChilliButton = CreateButton(
    "ICUS MOD BYPASS CHILLI",
    UDim2.new(0, 15, 0, 258)
)

--========================================================--
-- LOADER
--========================================================--

local function RunScript(URL)

    task.spawn(function()

        local Success, Result = pcall(function()

            local Source = game:HttpGet(URL)
            local Func = loadstring(Source)

            if not Func then
                error("Không thể load script.")
            end

            return Func()
        end)

        if not Success then
            warn("[Best Hub] Script error:", Result)
        end

    end)
end

--========================================================--
-- MIRANDA HUB
--========================================================--

MirandaButton.MouseButton1Click:Connect(function()

    MirandaButton.Text = "Loading..."

    RunScript(
        "https://raw.githubusercontent.com/miirandahub/loader/refs/heads/main/stealaeggs"
    )

    task.delay(1, function()
        MirandaButton.Text = "Miranda Hub"
    end)

end)

--========================================================--
-- LEMON HUB
--========================================================--

LemonButton.MouseButton1Click:Connect(function()

    LemonButton.Text = "Loading..."

    RunScript(
        "https://raw.githubusercontent.com/lennonxscripts/lennonhubv3/refs/heads/main/stealanegg.lua"
    )

    task.delay(1, function()
        LemonButton.Text = "Lemon Hub"
    end)

end)

--========================================================--
-- HOP SERVER
--========================================================--

HopButton.MouseButton1Click:Connect(function()

    HopButton.Text = "Loading..."

    RunScript(
        "https://raw.githubusercontent.com/nguyenthivan1984xx-stack/Andzhehe/refs/heads/main/Hop%20sever"
    )

    task.delay(1, function()
        HopButton.Text = "Hop Sever"
    end)

end)

--========================================================--
-- FIX LAG
--========================================================--

local FixLagEnabled = false
local GuardAnimators = {}

local function IsProtected(Object)

    local Current = Object

    while Current and Current ~= Workspace do

        local Name = string.lower(Current.Name)

        if string.find(Name, "egg")
        or string.find(Name, "guard") then
            return true
        end

        Current = Current.Parent
    end

    return false
end

local function IsCharacter(Object)

    local Model = Object:FindFirstAncestorOfClass("Model")

    if not Model then
        return false
    end

    return Model:FindFirstChildOfClass("Humanoid") ~= nil
end

local function GetGuardModel(Object)

    local Current = Object

    while Current and Current ~= Workspace do

        if Current:IsA("Model") then

            local Name = string.lower(Current.Name)

            if string.find(Name, "guard") then
                return Current
            end
        end

        Current = Current.Parent
    end

    return nil
end

--========================================================--
-- DỪNG ANIMATION GUARD
--========================================================--

local function StopGuardAnimations(GuardModel)

    if not GuardModel then
        return
    end

    local Humanoid = GuardModel:FindFirstChildOfClass("Humanoid")

    if not Humanoid then
        return
    end

    local Animator = Humanoid:FindFirstChildOfClass("Animator")

    if not Animator then
        return
    end

    for _, Track in ipairs(
        Animator:GetPlayingAnimationTracks()
    ) do

        pcall(function()
            Track:Stop(0)
        end)

    end

    if GuardAnimators[Animator] then
        return
    end

    GuardAnimators[Animator] = true

    Animator.AnimationPlayed:Connect(function(Track)

        if not FixLagEnabled then
            return
        end

        pcall(function()
            Track:Stop(0)
        end)

    end)
end

--========================================================--
-- XỬ LÝ OBJECT
--========================================================--

local function ProcessObject(Object)

    if not Object or not Object.Parent then
        return
    end

    local GuardModel = GetGuardModel(Object)

    if GuardModel then
        StopGuardAnimations(GuardModel)
        return
    end

    if IsCharacter(Object) then
        return
    end

    if IsProtected(Object) then
        return
    end

    if Object:IsA("BasePart") then

        if Object.CanCollide == false then

            Object.LocalTransparencyModifier = 1
            Object.CanQuery = false
            Object.CanTouch = false

        end

        pcall(function()
            Object.Color = Color3.fromRGB(255, 255, 255)
        end)

        Object.CastShadow = false
    end

    if Object:IsA("SpecialMesh") then

        pcall(function()
            Object.VertexColor = Vector3.new(1, 1, 1)
        end)

    end

    if Object:IsA("ParticleEmitter")
    or Object:IsA("Trail")
    or Object:IsA("Beam")
    or Object:IsA("Smoke")
    or Object:IsA("Fire")
    or Object:IsA("Sparkles") then

        Object.Enabled = false
    end
end

--========================================================--
-- CHẠY FIX LAG
--========================================================--

local function RunFixLag()

    if FixLagEnabled then
        return
    end

    FixLagEnabled = true

    pcall(function()
        Lighting.GlobalShadows = false
    end)

    for _, Effect in ipairs(
        Lighting:GetDescendants()
    ) do

        if Effect:IsA("PostEffect") then

            pcall(function()
                Effect.Enabled = false
            end)

        end
    end

    for _, Object in ipairs(
        Workspace:GetDescendants()
    ) do

        task.defer(function()

            if FixLagEnabled then
                ProcessObject(Object)
            end

        end)
    end

    Workspace.DescendantAdded:Connect(function(Object)

        if not FixLagEnabled then
            return
        end

        task.defer(function()

            if FixLagEnabled then
                ProcessObject(Object)
            end

        end)
    end)

    FixLagButton.Text = "Fix Lag: ON"
end

--========================================================--
-- FIX LAG BUTTON
--========================================================--

FixLagButton.MouseButton1Click:Connect(function()
    RunFixLag()
end)

--========================================================--
-- ICUS MOD BYPASS CHILLI
--========================================================--

ChilliButton.MouseButton1Click:Connect(function()

    ChilliButton.Text = "Loading..."

    task.spawn(function()

        local Success, Result = pcall(function()

            local URL =
                "https://raw.githubusercontent.com/robvxs24/freemium/refs/heads/main/chillviethoa.lua"

            local Source = game:HttpGet(URL)
            local Func = loadstring(Source)

            if not Func then
                error(
                    "Không thể load ICUS MOD BYPASS CHILLI."
                )
            end

            return Func()
        end)

        if not Success then

            warn(
                "[Best Hub] ICUS MOD error:",
                Result
            )

        end

        ChilliButton.Text =
            "ICUS MOD BYPASS CHILLI"

    end)
end)

--========================================================--
-- MỞ / ĐÓNG MENU
--========================================================--

OpenButton.MouseButton1Click:Connect(function()

    Main.Visible = not Main.Visible

end)
